<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobdeve.group11.assist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assist">
        <activity android:name=".CalendarDayActivity"></activity>
        <activity android:name=".EditEventActivity" />
        <activity android:name=".CalendarYearActivity" />
        <activity android:name=".AddEventActivity" />
        <activity android:name=".ViewEventActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CalendarMonthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>